<?xml version="1.0" encoding="utf-8"?><!-- TODO width match parent -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:paddingStart="0dp"
    android:paddingEnd="16dp">

    <TextView
        android:id="@+id/tvBtnExit"
        style="@style/pretendard_regular_12"
        android:layout_width="@dimen/chat_exit_width"
        android:layout_height="match_parent"
        android:background="@drawable/bg_rect_red_right_r10"
        android:gravity="end|center_vertical"
        android:paddingStart="0dp"
        android:paddingEnd="17dp"
        android:text="@string/exit"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>